# Optional dependencies for the local chest X-ray classifier.
# Install only when you've trained a model and want the backend to use it
# instead of the multimodal-LLM fallback at /xray.
#
#     pip install -r requirements-xray.txt
#
# The backend imports these lazily — the server still runs without them.

torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
pillow>=10.0.0
numpy>=1.26.0
pydicom>=2.4.0

# (Optional) ONNX runtime if you want to swap to xray.onnx for CPU inference:
# onnxruntime>=1.20.1
